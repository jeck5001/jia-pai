# 待裁决

一体化 NAS 服务已由服务端代理 Sub2API，浏览器和微信小程序不再直接访问内网模型服务。若后续开放公网或接入微信小程序，需要确定 HTTPS 域名、反向代理和访问控制方案。

## UI 升级遗留事项（2026-07-28）

- `index.html` 的 `theme-color` 仍是旧墨绿 `#114338`，与新深墨品牌色不一致。该文件不在本次允许修改清单内，未动。建议下次获准后改为 `#1d2126`（深墨），Android 状态栏与 PWA 外观即与新视觉一致。
- 本机 OrbStack 占用了 3000 端口，`npm run server` 与 vite dev 代理（`vite.config.ts` 固定指向 127.0.0.1:3000）在本机会冲突。验收改用 `PORT=3100 node server/index.mjs` 同源托管通过。NAS 部署不受影响；若需在本机联调 dev 代理，请先释放 3000 端口。

除上述两项外，本次 UI 升级无其他待裁决事项。
