# 进度

- 已核对：目录无工程和 Git 仓库，有 8 张受保护价格单照片；Node v24.3.0，npm 11.18.0。
- 目标：交付可部署到飞牛 NAS 的一体化价格查询服务，查询端通过同源 API 读取已发布的 `products.json`。
- 顺序：先定义价格数据契约和查询，再完成本地导入/校对/导出，最后接入服务端持久化和视觉模型代理。
- 已知限制：照片 25、26、27、29、30、31、32 未标明表格日期；同一商品可能有不同报价，购买前须由小卖部管理员确认现行价格。
- 视觉方向：纸张白与墨绿的紧凑工具界面，价格信息优先，移动端单手查询。
- 已完成：静态 PWA 查询、扫码入口、本地 Excel/CSV 导入校对、JSON 导出、中文运维说明，以及从 8 张照片整理并去重后的 175 条来源报价。相同 Item ID 且同价、或相同商品名且同价的记录只保留一条；不同价格仍会分别显示来源。
- 已完成：新增 NAS 一体化 Node 服务，托管前端、持久化 `data/products.json`、提供管理员口令保护的目录发布 API，并由服务端调用 Sub2API 识别照片。前端不再接触模型地址或 API Key，导入和照片校对完成后可直接发布到 NAS。
- 已完成：新增 Dockerfile、Compose、`.env.example` 和飞牛 NAS 部署说明；容器通过挂载 `./data:/app/data` 保留价格表。

## Android App 进度

- 2026-07-28 任务 0 已复核：`npm run test` 为 5 个文件、17 项测试全绿；`npm run build` 成功并转换 1857 个模块。
- 目标：用 Capacitor 打包现有 React 界面为 Android App，并由 GitHub Actions 云端生成可侧载 debug APK。
- 顺序：先实现仅原生端需要的 NAS 地址配置与 API 路径，再接入 Android 工程，最后补 CI、APK 校验和文档。
- 最大风险：Android WebView 访问内网 HTTP 和跨域 API；使用 CapacitorHttp 原生请求补丁及 Android 的内网 HTTP 配置处理，网页端继续同源请求。
- 已完成：Capacitor 8.4.2 Android 工程、`com.jeck5001.jiapai` 应用 ID、联网/相机权限、原生 NAS 首次连接页和可重开设置入口。
- 已完成：新增地址解析单测；网页端固定保留相对 `/api`，原生端保存并验证 NAS 根地址后才使用绝对 API 地址。
- 已完成：新增 GitHub Android 工作流，云端执行测试、Web 构建、Capacitor 同步、`assembleDebug lintDebug`、APK 身份和权限校验，上传 30 天 APK Artifact。
- 反向验证：临时将新增地址测试改为允许非法地址后，`npm run test -- src/lib/app-config.test.ts` 如预期为 1 个文件失败、4 项失败，覆盖 `ftp://...` 和带 `?debug=1` 的地址；还原断言后同一命令为 1 个文件、7 项全绿。
- CI 第 1 次构建：`assembleDebug` 已成功，`lintDebug` 因 CAMERA 权限缺少非必需硬件声明失败；已补 `android.hardware.camera required=false`，不使用 lint baseline 或关闭 lint 绕过。

## UI/交互升级（2026-07-28 第二次会话）

- 任务 0 基线复核：`npm run test` 7 个文件 25 项全绿；`npm run build` 成功；`npm run android:sync` 成功，与领导基线一致。
- 目标：把"小卖部查价"升级为日常高频、精致前卫的新零售查价工具，可用性 > 视觉品质 > 开发速度。
- 顺序：先 CSS 变量视觉系统，再员工查价体验，再管理员三工作台统一，最后三视口实机验收。
- 视觉方向：瓷白与深墨为底，信号橙作价格与关键动作色，鲜绿仅表成功；无紫色渐变、无拟物卡片、无营销大 Hero。
- 最大风险：移动端导入/照片识别草稿表格横滚是主路径——改为 760px 以下卡片式逐行编辑（沿用商品管理既有模式），业务处理函数不变。
- 决策：触控目标统一 ≥44px；桌面表格内鼠标路径的输入框视觉高度可低于 44px，手机端编辑全部走卡片布局并满足 44px。
- 决策：验收用系统 Chrome + 隔离环境的 playwright-core 驱动，不进入项目依赖；原生 NAS 设置页用注入 `window.Capacitor` 的方式真实渲染检查。
- 已完成任务 1：`src/styles.css` 全部重写为 CSS 变量体系（瓷白/深墨/信号橙/鲜绿、4px 间距阶、8-20 圆角阶、三层阴影、120/200/280ms 动效阶），删除互相覆盖的旧样式；触控目标 ≥44px；`:focus-visible` 信号橙焦点环；`prefers-reduced-motion` 全局降级保留。
- 已完成任务 2：查价首屏去掉大 Hero，标题压缩为一行 + 目录元信息（来源 · 生效时间 · 在售条数），搜索框与扫码按钮成为第一屏主角；结果行名称/规格完整换行不截断，价格 25px 信号橙等宽数字为全行最醒目元素；扫码反馈区分成功/警示（图标+文字）；最近查询改为 44px 胶囊。
- 已完成任务 3：三个工作台统一标题区/字段 `.field`/分段选择器/校验列表（图标化）/草稿工具栏/发布提示（`.notice` 图标+文字，紧贴发布按钮）；商品管理发布栏从长表格底部上移到顶部工具栏；≤860px 三个工作台草稿全部改卡片式逐行编辑（字段按阅读顺序），桌面保留紧凑表格；照片识别"定位此行"改为 `data-candidate-row` 取可见元素并遵守 reduced-motion。
- 中间验证：任务 1-3 完成后 `npm run build` 成功、`npm run test` 25 项全绿；类名交叉核对 101 个使用类全部有样式（2 个为沿用旧有的无样式钩子类）。
- 已完成任务 4：系统 Chrome 实测 390x844 / 768x1024 / 1440x900 三视口，覆盖查价（默认/结果/最长名换行/无结果/最近查询/加载/错误/空目录/扫码弹窗）、表格导入（默认/草稿/预览/发布失败/校验错误）、照片识别（默认/预览条/识别失败）、商品管理（表格与卡片/筛选/发布失败）、NAS 设置（首次连接/非法地址/连接成功/重开取消）。
- 自动化断言：三视口均无意外横向滚动、无名称/规格截断、名称与价格不重叠、价格字号恒大于名称、触控目标 ≥44px；`prefers-reduced-motion` 下动画/过渡实测降为 0.01ms。
- 验收中发现并修复：照片预览条缺样式导致原图 3072px 撑破页面（补 CSS 并重建）；`.text-button` 40px 不达标（升 44px）。修复后全部检查通过。
- 验收纪律：发布按钮全程在拦截 PUT /api/catalog 下测失败提示，`data/products.json` 验收前后 SHA-256 一致（a80cf337…），未写入任何数据；截图与验收脚本均在 /tmp，未进仓库。
- 最终基线：`npm run test` 7 文件 25 项全绿；`npm run build` 成功（1859 模块）；`npm run android:sync` 成功。改动仅限 `src/App.tsx`、`src/styles.css`、`src/components/` 三个工作台与 PROGRESS/BLOCKED。
